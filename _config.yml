title: Python Training
description: Structured Python, ML, DL, and LLM curriculum
theme: minima

defaults:
  - scope:
      path: ""
    values:
      layout: default

plugins:
  - jekyll-relative-links

relative_links:
  enabled: true
  collections: true

# Make sure markdown files are published
include:
  - README.md